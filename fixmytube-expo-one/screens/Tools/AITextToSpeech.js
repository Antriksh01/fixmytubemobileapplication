import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AITextToSpeech = () => {
  return (
    <View>
      <Text>AITextToSpeech</Text>
    </View>
  );
};

export default AITextToSpeech;

const styles = StyleSheet.create({});
